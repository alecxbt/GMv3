name = "gm-terminal-api"
main = "src/worker.ts"
compatibility_date = "2024-06-07"

[site]
bucket = "../frontend/dist"

[vars]
FRONTEND_URL = "http://localhost:3000"
PORT = "5001"

# Secrets to set via `wrangler secret put`:
# DATABASE_URL
# JWT_SECRET